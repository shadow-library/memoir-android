package com.shadow.apps.memoir.domain.model

/*
 * Startup destination
 *
 * Domain-level app entry decision. Navigation maps these values to routes.
 */
enum class StartupDestination {
    GettingStarted,
    SignIn,
    DeviceSetup,
    Home,
}
